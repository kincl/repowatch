---
- project: kincl/repowatch
  type: gitlab
  path: __testing_repowatch
  cmds:
    - echo "%{branch} %{branchdir}"
    - echo "%{project} %{projectdir}"
- project: github/orchestrator
  type: gitlab
  path: __testing_github_orchestrator
  cmds:
    - echo "%{branch} %{branchdir}"
    - echo "%{project} %{projectdir}"
    - sleep 2
